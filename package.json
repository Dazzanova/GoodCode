{
  "name": "goodcode",
  "version": "1.0.0",
  "private": true,
  "description": "Practice with intent - A production-quality DSA learning platform",
  "scripts": {
    "dev": "npm -C apps/web run dev",
    "build": "npm -C apps/web run build",
    "start": "npm -C apps/web run start",
    "lint": "npm -C apps/web run lint",
    "db:generate": "npm -C apps/web run db:generate",
    "db:migrate": "npm -C apps/web run db:migrate",
    "db:seed": "npm -C apps/web run db:seed",
    "db:studio": "npm -C apps/web run db:studio"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AnandNair4/GoodCode.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prisma": "^8.0.0-rc.12"
  },
  "dependencies": {
    "@prisma/client": "^7.10.0"
  }
}
